info:
  name: "CREATE_PERMISSION "
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/permissions"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
