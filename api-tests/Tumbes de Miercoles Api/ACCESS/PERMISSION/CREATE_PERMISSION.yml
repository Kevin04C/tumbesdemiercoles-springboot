info:
  name: "CREATE_PERMISSION "
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/permissions"
  body:
    type: json
    data: |2-
      
        {
          "name": "READ_ALL_CATEGORY",
          "description": "Permite ver la lista de todas las categorias"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # 📌 Endpoint: "CREATE_PERMISSION "
  
  ## 📝 Descripción
  Endpoint para la operación "CREATE_PERMISSION ".
  
  ## 🌐 Detalles Técnicos
  - **Método HTTP**: `POST`
  - **Ruta/Endpoint**: `"{{url}}/api/v1/permissions"`
  - **Autenticación**: Requerido (Bearer Token JWT)
  
  ## ⚙️ Parámetros y Filtros
  Ver URL / Path Variables.
  
  ## 📦 Cuerpo de la Petición (Request Body)
  N/A
  
  ## 📤 Respuesta (Response)
  Respuesta exitosa de la API en formato ApiResponse.
