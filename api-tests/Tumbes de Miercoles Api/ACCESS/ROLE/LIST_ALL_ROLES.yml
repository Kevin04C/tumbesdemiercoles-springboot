info:
  name: LIST_ALL_ROLES
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
