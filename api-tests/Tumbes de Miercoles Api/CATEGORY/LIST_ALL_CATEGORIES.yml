info:
  name: LIST_ALL_CATEGORIES
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
