info:
  name: LIST_COLUMNISTS
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/columnist?page=0&size=10&sortDir=DESC&sortBy=createdAt"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # 📌 Endpoint: LIST_COLUMNISTS
  
  ## 📝 Descripción
  Lista de forma paginada y ordenada por fecha las columnas de opinión publicadas.
  
  ## 🌐 Detalles Técnicos
  - **Método HTTP**: `GET`
  - **Ruta/Endpoint**: `"{{url}}/api/v1/columnist?page=0&size=10&sortDir=DESC&sortBy=createdAt"`
  - **Autenticación**: Público
  
  ## ⚙️ Parámetros y Filtros
  Parámetros de paginación.
  
  ## 📦 Cuerpo de la Petición (Request Body)
  Ninguno
  
  ## 📤 Respuesta (Response)
  Colección paginada de columnas.
