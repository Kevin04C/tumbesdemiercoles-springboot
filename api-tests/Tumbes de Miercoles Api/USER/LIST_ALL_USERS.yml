info:
  name: LIST_ALL_USERS
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
