info:
  name: LIST_ALL_USERS
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/v1/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
